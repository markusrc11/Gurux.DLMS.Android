package gurux.dlms.objects.enums;

/*
 * Defines capture method of compact data object.
 */
public enum CaptureMethod {
    /**
     * Data is captured with Capture-method.
     */
    INVOKE,
    /**
     * Data is captured upon reading.
     */
    IMPLICIT
}
